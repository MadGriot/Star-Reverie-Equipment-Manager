<Window x:Class="Star_Reverie_Inventory_Manager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Star_Reverie_Inventory_Manager"
        mc:Ignorable="d"
        Title="Star Reverie Database Manager" Height="450" Width="600">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Create Item">
                <MenuItem Header="_Create Weapon" Click="CreateWeaponButton_Click" />
                <MenuItem Header="_Create Armor" Click="CreateArmorButton_Click"/>
                <MenuItem Header="_Create Shield" Click="CreateShieldButton_Click"/>
                <MenuItem Header="_Create Ammo" Click="CreateAmmoButton_Click"/>
                <MenuItem Header="_Create Battery" Click="CreateBatteryButton_Click"/>
                <MenuItem Header="_Create First Aid" Click="CreateFirstAidButton_Click"/>
                <Separator />
            </MenuItem>
            <MenuItem Header="_Create Techniques">
                <MenuItem Header="_Create Astral Technique" Click="CreateAstralTechniqueButton_Click" />
                <MenuItem Header="_Create Technique" Click="CreateTechniqueButton_Click" />
            </MenuItem>
            <MenuItem Header="_Display Items">
                <MenuItem Header="_Display Weapons" Click="DisplayWeaponsButton_Click" />
                <MenuItem Header="_Display Armors" Click="DisplayArmorsButton_Click" />
                <MenuItem Header="_Display Shields" Click="DisplayShieldsButton_Click" />
                <MenuItem Header="_Display Techniques" Click="DisplayTechniqueButton_Click"/>
                <MenuItem Header="_Display Astral Techniques" Click="DisplayAstralTechniqueButton_Click" />
            </MenuItem>
            <MenuItem Header="_Character Manager">
                <MenuItem Header="_Create Character" Click="CreateCharacterButton_Click"/>
                <MenuItem Header="_Create Squad" Click="CreateSquadButton_Click"/>
                <MenuItem Header="_Create Platoon" Click="CreatePlatoonButton_Click"/>
                <MenuItem Header="_Display Platoons" Click="DisplayPlatoonsButton_Click"/>
            </MenuItem>
            <MenuItem Header="_Quest Manager">
                <MenuItem Header="_Create Quest" Click="CreateQuestButton_Click" />
                <MenuItem Header="_Display Quests" />
            </MenuItem>
        </Menu>
        <StackPanel Margin="10">
            <TextBox Margin="0 5 0 5"
                 TextChanged="TextBox_TextChanged"/>
            <ListView x:Name="ItemsListView"
                  SelectionChanged="ItemsListView_SelectionChanged"
                  Height="280"
                  Margin="0 5 0 0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock FontSize="20" Foreground="DarkCyan" FontWeight="Bold">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} {1}">
                                        <Binding Path="FirstName" />
                                        <Binding Path="LastName" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="15" FontWeight="Bold" Text="Equipped Weapon: "/>
                                <TextBlock FontSize="15" Text="{Binding Weapon.Name}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontSize="15" FontWeight="Bold" Text="Equipped Armor: "/>
                                <TextBlock FontSize="15" Text="{Binding Armor.Name}"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </DockPanel>

</Window>
