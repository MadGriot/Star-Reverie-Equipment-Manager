<UserControl x:Class="Star_Reverie_Inventory_Manager.Controls.SkillControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="800"
             Background="#160a26">

    <UserControl.Resources>
        <!-- Button Style for +/- -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1c0f30"/>
            <Setter Property="Foreground" Value="#c084fc"/>
            <Setter Property="BorderBrush" Value="#b14cff"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2a1245"/>
                    <Setter Property="Foreground" Value="#ffffff"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBlock for numbers -->
        <Style x:Key="NumberTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- TextBlock for labels -->
        <Style x:Key="LabelTextBlock" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#c084fc"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Border Background="#1c0f30" BorderBrush="#301a4d" BorderThickness="1" CornerRadius="5" Padding="5" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Subtract Skill Button -->
            <Button x:Name="SubtractSkillButton" Click="SubtractSkillButton_Click" Content="-" Margin="0 0 5 0"/>

            <!-- Skill Name -->
            <TextBlock x:Name="nameTextBlock" Grid.Column="1" Text="Skill Name" Style="{StaticResource LabelTextBlock}"/>

            <!-- Skill Level -->
            <TextBlock x:Name="skillLevel" Grid.Column="2" Text="0" Style="{StaticResource NumberTextBlock}" HorizontalAlignment="Center"/>

            <!-- Add Skill Button -->
            <Button x:Name="AddSkillButton" Grid.Column="3" Click="AddSkillButton_Click" Content="+" Margin="5 0 0 0"/>
        </Grid>
    </Border>
</UserControl>
